<?php
    echo(pi());

    // php min() and max functions 
    echo(min(0,150,30,20,-8,-200));
    echo(max(0,150,30,20,-8,-200)); 

    // php abs function || abs() function return the absolute(positive) value of a number;
    echo(abs(-6.7));

    // sqrt() function return the square root of a number
    echo(sqrt(64));

    // round() function returns rounds a floating point numbers to its nearest integer;
    echo(round(0.60));
    echo(round(0.49));

    // rand() function generates a random number
    echo(rand(10,100));
?>